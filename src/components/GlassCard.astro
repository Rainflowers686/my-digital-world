---
interface Props {
  class?: string;
  as?: 'div' | 'article';
}

const { class: extraClass = '', as = 'div' } = Astro.props;
const Tag = as;
---

<Tag class:list={['glass-card', extraClass]}>
  <slot />
</Tag>
